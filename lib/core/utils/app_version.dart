/// Versión actual de la aplicación.
/// Debe coincidir con la versión declarada en pubspec.yaml (antes del +).
/// Actualizar este valor junto con pubspec.yaml en cada release.
const String kAppVersion = '1.0.2'; //1.0.1

/// Número de build. Corresponde al valor después del + en pubspec.yaml.
/// Ej: versión 1.0.2+3 → kAppBuild = '3'
const String kAppBuild = '3';
